/** Solid brand-toned gradient shown when an episode or playlist has no uploaded cover. */
export const FALLBACK_COVER_GRADIENT =
  'linear-gradient(140deg, hsl(var(--aura-1)) 0%, hsl(var(--aura-2)) 60%, hsl(var(--aura-4)) 100%)';
